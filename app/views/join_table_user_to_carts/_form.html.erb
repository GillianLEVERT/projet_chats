<%= form_with(model: join_table_user_to_cart, class: "contents") do |form| %>
  <% if join_table_user_to_cart.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(join_table_user_to_cart.errors.count, "error") %> prohibited this join_table_user_to_cart from being saved:</h2>

      <ul>
        <% join_table_user_to_cart.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inline">
    <%= form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>

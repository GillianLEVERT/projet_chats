<div class="px-4 sm:px-6 lg:px-8 py-8 w-full max-w-9xl mx-auto">
  <p style="color: green"><%= notice %></p>
  
  <div id="items" class="grid grid-cols-12 gap-6">
    <% @items.each do |item| %>
    <%= render item %>
    <% end %>
  </div>
</div>